package types

const (
	// Event types for precisebank operations
	EventTypeFractionalBalanceUpdated = "fractional_balance_updated"

	// Attribute keys (reuse existing ones for consistency)
	AttributeKeyAddress = "address"
	AttributeKeyDelta   = "delta"
)
